import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

interface Props {
    
}

const RegisterSneakerScreen = (props: Props) => {
    return (
        <View>
            <Text>Màn hình đăng kí</Text>
        </View>
    )
}

export default RegisterSneakerScreen

const styles = StyleSheet.create({})
